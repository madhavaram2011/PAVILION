PORT=5000
NODE_ENV=development

# JWT — Access token (short lived)
JWT_SECRET=pavilion_super_secret_key_2024
JWT_EXPIRES_IN=7d

# JWT — Refresh token (httpOnly cookie, long lived)
JWT_REFRESH_SECRET=pavilion_refresh_secret_key_2024
JWT_REFRESH_EXPIRES=7d

# MongoDB
MONGODB_URI=mongodb://localhost:27017/pavilion# Client origin (for CORS credentials)
CLIENT_URL=http://localhost:5173

# Google Maps API (optional — fallback coords used without it)
# GOOGLE_API_KEY=your_google_api_key_here
